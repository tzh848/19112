<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.st.dao.ITipDao">

<select id="loginUser_tip_count" parameterType="map" resultType="int">
select count(*) from tip where tip_state=0 and tip_user_id=#{userid}
</select>


</mapper>